[project]
name = "agenticai-iv-app"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "Flask",
  "flask-cors",
  "openai",
  "pypdf",
  "psycopg[binary]",
  "supabase",
]

[project.scripts]
app = "iv_agent.app:app"
